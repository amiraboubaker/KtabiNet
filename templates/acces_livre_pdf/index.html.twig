{% extends 'base.html.twig' %}
    
{% block title %}Access{% endblock %}
    
{% block stylesheets %}
<link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
{% endblock %}

{% block body %}
<div class="flex justify-center items-center min-h-screen">
    <div class="form-container bg-white rounded-lg shadow-md p-8 max-w-md w-full">
        <h1 class="form-title text-2xl font-bold text-gray-800 mb-6">Request Access for {{ livrePdf.Titre }}</h1>
        <div class="image-container mb-6">
            <img src="{{ livrePdf.UrlImage }}" alt="{{ livrePdf.Titre }}" class="w-full rounded-lg">
        </div>
        <div hidden>
        <form action="{{ path('request_access', {'id': livrePdf.id}) }}" method="post">
            <button type="submit" class="form-submit bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-full transition duration-300">Submit Request</button>
        </form>
        </div >
        <form action="{{ path('request_access', {'id': livrePdf.id}) }}" method="post">
            <button type="submit" class="form-submit bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-full transition duration-300">Submit Request</button>
        </form>
        <a href="{{ path('app_mes_livres_pdf') }}" class="go-back text-gray-600 mt-4 block text-sm hover:text-gray-700 transition duration-300">Go Back</a>
    </div>
</div>
{% endblock %}
